[project]
name = "poetry-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "langchain-openai>=0.3.32",
    "poetry-ai",
]

[tool.uv.sources]
poetry-ai = { path = "../poetry-ai" }
